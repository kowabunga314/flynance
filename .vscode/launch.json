{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Django: Attach",
        "type": "python",  // Use "python" or "debugpy" as supported
        "request": "attach",
        "preLaunchTask": "docker-up-debugger",
        "connect": {
          "host": "localhost",
          "port": 5678
        },
        "pathMappings": [
          {
            "localRoot": "${workspaceFolder}/aircraft_api",
            "remoteRoot": "/app"
          }
        ],
        "justMyCode": true
      }
    ]
  }
  